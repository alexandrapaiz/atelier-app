# Pending — fixes the exo seat cannot reach

Exo-owned. Everything here is a fix that belongs in the tree but sits
outside what this seat's token or permissions can change. Each entry
carries exact ready-to-paste content so the owner can apply it without
rediscovering the problem. An entry leaves this file only when the fix
is in the tree, never because it got stale.

Format:

```
### P<n> — <one-line ask> (YYYY-MM-DD)
- Blocked by: <token scope | owner-only secret | charter rule | other>
- Where: <exact file and location>
- Paste this:
  <content>
- Why: <the incident or lesson this closes>
```

*No pending items yet. The register exists before the seats run.*
